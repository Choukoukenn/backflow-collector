#include "Main.h"
#include "Base.h"

//---------------------------------------------------------------------------------
//	コンストラクタ
//---------------------------------------------------------------------------------
Base::Base()
{
	m_model = -1;     // モデル番号を未初期化状態（-1）に設定
	m_pos.Clear();    // 座標（X, Y, Z）をゼロクリア
	m_rot.Clear();    // 回転角度（X, Y, Z）をゼロクリア
}